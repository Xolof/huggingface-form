name: PHP Composer

on:
  push:
    branches: [ "develop", "github-workflow" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # - name: Validate composer.json and composer.lock
    #   run: composer validate --strict

    - name: Cache Composer packages
      id: composer-cache
      uses: actions/cache@v3
      with:
        path: vendor
        key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
        restore-keys: |
          ${{ runner.os }}-php-

    - name: Install dependencies
      run: composer install --prefer-dist --no-progress

    # Add a test script to composer.json, for instance: "test": "vendor/bin/phpunit"
    # Docs: https://getcomposer.org/doc/articles/scripts.md

    - name: Run PhpCS
      run: vendor/bin/phpcs src tests -s

    - name: Run PhpCbf
      run: vendor/bin/phpcbf src tests

    - name: Run PhpMd src dir
      run: vendor/bin/phpmd src text cleancode

    - name: Run PhpMd tests dir
      run: vendor/bin/phpmd tests text cleancode

    - name: Run PhpStan
      run: vendor/bin/phpstan analyse src tests --memory-limit="512M" --level=5

    - name: Run unit tests
      run: vendor/bin/phpunit tests
